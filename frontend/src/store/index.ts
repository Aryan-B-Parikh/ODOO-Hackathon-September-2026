/**
 * AssetFlow ERP
 *
 * Layer:
 * Presentation (Store)
 *
 * Responsibility:
 * Client-side local state store bootstrap (e.g., Zustand).
 *
 * Architectural Rules:
 * - No server state here (handled by React Query).
 * - Only global UI state (e.g., sidebar open/close).
 */

export const useAppStore = () => {
  // Placeholder for Zustand store
};
